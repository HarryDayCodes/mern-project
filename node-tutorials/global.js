// GLOBAL

// This function is run once after 3.5 seconds (3500 ms)
// setTimeout(() => {  
//     console.log('in the timeout')
//     clearInterval(int)  // It then clears the 'set Interval' function
// }, 3500);

// This function is run EVERY 1 second
// const int = setInterval(() => {
//     console.log('in the interval')
// }, 1000)

console.log(__dirname); // Absolute folder of path we're in
console.log(__filename); // __dirname WITH current file added on

